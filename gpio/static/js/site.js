var socket = io();
socket.on('status update', function(msg){

});
$(document).ready(() => {
    // $.post( "/", { action: "status" }, function( data ) {});
    function init() {
    
    }
    // $("#stop").click(() => {
    //     $.post( "/", { action: "stop" }, function( data ) {
    //         console.log( data );
    //     });
    // });
})
